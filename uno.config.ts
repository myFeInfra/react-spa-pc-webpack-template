import { defineConfig } from 'unocss'

export default defineConfig({
  // 可以根据需要添加规则、预设等配置
  rules: [
    // 示例规则
    ['m-1', { margin: '0.25rem' }]
  ],
  presets: [
    // 可以添加预设，例如预设的 Tailwind 类
  ]
})
